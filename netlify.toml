[build]
  command = "npm run build"
  publish = "dist"
  # Sin `ignore`: auto-deploy al pushear a main. El quality gate de GitHub Actions actúa
  # como guardia antes del merge — solo llega a main código que pasa CI.

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
