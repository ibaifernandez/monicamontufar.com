[build]
  command = "npm run build"
  publish = "dist"
  ignore = "exit 0" # Bloquea explícitamente despliegues automáticos para ahorrar build minutes

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
