# _redirects configuration
# Block sensible paths
/.env  404
/.git* 404
/wp-admin* 404
/wp-login* 404
/xmlrpc.php 404

# Fallback 404: devuelve home con status 404 (SSG no es SPA — preserva semántica HTTP para SEO)
/* /index.html 404
